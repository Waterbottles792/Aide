# Mentor AI — Phase 1 Scaffold

This repository contains the Phase 1 scaffold for Mentor AI — a desktop GenAI mentor for cybersecurity labs.

Run the backend (development):

```powershell
python -m uvicorn mentor_ai.app.backend.main:app --reload --port 8000
```

Note: replace command with correct module path if running from workspace root.
